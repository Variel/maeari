import { FormField } from "../types/FormField";

interface FormMessageViewProps {
  fields: FormField[];
}

const FormMessageView: React.FC<FormMessageViewProps> = ({ fields }) => {
  return <></>;
};

export default FormMessageView;
